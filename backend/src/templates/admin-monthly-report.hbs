<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Monthly Seller Statistics Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background-color: #2563eb;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 8px;
        }

        .content {
            padding: 20px;
            background-color: #ffffff;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            margin-top: 20px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }

        .stat-card {
            background-color: #f8fafc;
            padding: 15px;
            border-radius: 8px;
            border-left: 4px solid #2563eb;
        }

        .stat-number {
            font-size: 24px;
            font-weight: bold;
            color: #2563eb;
        }

        .stat-label {
            font-size: 14px;
            color: #64748b;
            margin-top: 5px;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        .table th,
        .table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #e5e7eb;
        }

        .table th {
            background-color: #f8fafc;
            font-weight: 600;
        }

        .table tr:hover {
            background-color: #f8fafc;
        }

        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
            font-size: 12px;
            color: #64748b;
            text-align: center;
        }

        .currency {
            color: #059669;
            font-weight: 600;
        }
    </style>
</head>

<body>
    <div class="header">
        <h1>📊 Monthly Seller Statistics Report</h1>
        <p>{{reportMonth}} - Admin Dashboard</p>
    </div>

    <div class="content">
        <p>Hello {{adminName}},</p>

        <p>Here is your monthly seller statistics report for <strong>{{reportMonth}}</strong>:</p>

        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-number">{{totalSellers}}</div>
                <div class="stat-label">Active Sellers</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">{{totalAuctions}}</div>
                <div class="stat-label">Total Auctions Created</div>
            </div>
            <div class="stat-card">
                <div class="stat-number currency">${{totalRevenue}}</div>
                <div class="stat-label">Total Platform Revenue</div>
            </div>
        </div>

        {{#if sellerStats.length}}
        <h3>📈 Seller Performance Details</h3>
        <table class="table">
            <thead>
                <tr>
                    <th>Seller</th>
                    <th>Email</th>
                    <th>Auctions Created</th>
                    <th>Completed Auctions</th>
                    <th>Products Listed</th>
                    <th>Revenue Generated</th>
                </tr>
            </thead>
            <tbody>
                {{#each sellerStats}}
                <tr>
                    <td>{{sellerName}}</td>
                    <td>{{sellerEmail}}</td>
                    <td>{{totalAuctions}}</td>
                    <td>{{completedAuctions}}</td>
                    <td>{{totalProducts}}</td>
                    <td class="currency">${{totalRevenue}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{else}}
        <div style="text-align: center; padding: 40px; background-color: #f8fafc; border-radius: 8px;">
            <p style="color: #64748b; font-size: 16px;">No sellers created auctions this month.</p>
        </div>
        {{/if}}

        <p style="margin-top: 30px;">This report was automatically generated on {{reportMonth}} at 9:00 AM.</p>
    </div>

    <div class="footer">
        <p>This is an automated email from Bid Market Admin Dashboard.</p>
        <p>&copy; 2025 Bid Market. All rights reserved.</p>
    </div>
</body>

</html>
